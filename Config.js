const path              = require('path');
const fs                = require('fs');

module.exports = {
    VERSION: '1.0.0',
    path:{
        raw_code: path.join( __dirname, 'raw_code/' ),
    }

}